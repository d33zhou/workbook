/**
 * @param {number} n
 * @return {number}
 */
const climbStairs = function (n) {
  let counter = 0;

  if (n <= 1) {
    return n;
  } else {
    //recursive
  }
};

console.log(climbStairs(2)); // 2
console.log(climbStairs(3)); // 3
